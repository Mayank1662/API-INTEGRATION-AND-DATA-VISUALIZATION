

A tiny demo chatbot that recognises user intent with spaCy’s similarity
vectors and replies with pre-written responses.

## 💾 Setup

```bash
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
python -m spacy download en_core_web_md  # optional – script auto-downloads
